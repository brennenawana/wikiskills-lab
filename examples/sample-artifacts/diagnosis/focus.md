# Chosen focus

*(Format sample — see the folder README. Illustration only.)*

- **Focus (agreed with the user):** cut what the tracker pull adds to the
  prompt, without losing anything the task actually needs.
- **Why:** finding F1 — measured on the observed ticket, repeats on every
  ticket, and fully mechanical to measure from the ledgers. Scores:
  measurability 5, impact 5, effort 4. The user's own first concern (F3)
  stays on the list; it needs proper measurement first and is the natural
  second engagement.
- **Metric sketch (step 4 makes it exact):** tracker tokens carried into
  the prompt per ticket, lower is better, with a guard metric — the task
  must still succeed — so the improvement cannot win by deleting context
  the task needed.
- **Likely artifact kinds:** a fetch policy for the pipeline (which linked
  tickets, how many comments, summarize-then-include), maybe a skill file
  describing when to pull more.
